---
import ImageMod from "@/components/ImageMod.astro";
import Base from "@/layouts/Base.astro";
import { markdownify } from "@/lib/utils/textConverter";
import { getEntry } from "astro:content";

const about = await getEntry("about", "-index");
const { Content } = await about.render();
const { title, description, meta_title, image } = about.data;
---

<Base
  title={title}
  meta_title={meta_title}
  description={description}
  image={image}
>
  <section class="section-sm">
    <div class="container">
      <div class="row justify-center">
        <div class="text-center md:col-10 lg:col-7">
          <h2 set:html={markdownify(title)} class="h3 mb-6" />
          <div class="content">
            <!-- <Content /> -->
            we’re diving deep into the story behind each
            bite, revealing the hidden journey of food
            waste through mind-blowing Augmented
            Reality.
          </div>
          <div>
            <img src="/images/FCI_Booth.png" alt="Plate" />
          </div>
        </div>
      </div>
    </div>
  </section>
</Base>
